import { MakeSquareDirective } from './make-square.directive';

describe('MakeSquareDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeSquareDirective();
    expect(directive).toBeTruthy();
  });
});
